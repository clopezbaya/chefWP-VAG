---
driver:
  name: vagrant

provisioner:
  name: chef_zero

verifier:
  name: inspec

platforms:
  - name: ubuntu-18.04

suites:
  - name: wordpress-app
    run_list:
      - recipe[apache2::default]
    verifier:
      inspec_tests:
        - test/integration/wordpress-app
